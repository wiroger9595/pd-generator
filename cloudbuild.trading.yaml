steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - -f
      - Dockerfile.trading
      - -t
      - '$_IMAGE'
      - .
images:
  - '$_IMAGE'
options:
  machineType: 'E2_HIGHCPU_8'
  diskSizeGb: 100
